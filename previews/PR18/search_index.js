var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#HTTP-API","page":"API","text":"The Gantt server exposes a small REST API (all under /api/); the frontend is just another client. GET /api/rev returns a monotonic revision for cheap change polling; project CRUD lives under /api/projects. Analytics endpoints: /api/activity, /api/projects/{id}/scurve, /api/projects/{id}/workload, /api/projects/{id}/export.csv, /api/projects/{id}/export.ics (milestones and deadlines as an iCalendar document — see icalendar) and /api/projects/{id}/chart?fmt=png|pdf|svg (needs a Makie backend).\n\nGET /api/projects/{id}/cpm carries the CPM analysis (finish, per-task slack, critical flags) and, when any task has a three-point estimate, a pert object with expected, sd_days, estimated and p80 — the status bar's probabilistic finish, on the request the UI already makes. It is null when nothing has been estimated. POST /api/projects/{id}/pert applies the estimates (pert!: duration becomes (o + 4m + p)/6) and answers with the updated project, like /schedule; 409 when no task carries an estimate.\n\n/api/projects/{id}/workload is workload shaped for drawing: a contiguous day window (start, days) and, per person, a dense load vector with the number of simultaneous tasks on each day of the window (plus effort, peak, busy_days, over_days and the tasks themselves). Leaf tasks with no assignee come back under the \"\" key. It answers 409 when the project uses a business-day calendar and BusinessDays is not loaded on the server, like /cpm.\n\nWith Perth.run(share = true, key = \"...\"), non-host machines must append ?key=... to API calls, to /background and to the /ws presence socket. The page itself (HTML, JS, CSS) is served without the key — it is an empty shell until the API answers, and it is what asks for the key when the link came without one.\n\nGET /api/share reports the current sharing state — the URLs to hand out, a QR matrix for the LAN link, and whether the caller is the host. POST /api/share with {\"on\": true|false} flips it live; only the machine running the server may call it (403 otherwise, 409 if the server was started with an explicit host). POST /api/key with {\"key\": \"…\"} sets the access key the same way (\"\" drops it) and answers with the /api/share payload, since the links carry the key; also host-only, 409 with the server down. While sharing is off, every other route answers 403 to any machine but this one. The kanban server exposes the same two endpoints on its own port.","title":"HTTP API"},{"category":"section","location":"gantt/#Gantt","page":"Gantt","text":"","title":"Gantt"},{"category":"type","location":"gantt/#Perth.GanttTask","page":"Gantt","text":"GanttTask(; name, start, kwargs...)\n\nA single task (or milestone) on the Gantt chart.\n\nFields\n\nid::String: short unique identifier (auto-generated).\nname::String: task label shown on the chart.\nstart::Date: first day of the task.\nduration::Int: duration in calendar days (≥ 1). Milestones ignore this.\nprogress::Int: completion percentage, 0–100.\ndependencies::Vector{String}: predecessor references. Plain ids mean finish-to-start; \"id+3\"/\"id-2\" add lag/lead in days; \"SS:id\" and \"FF:id\" (optionally with lag) declare start-to-start / finish-to-finish.\ncost::Float64: planned cost (any unit). 0 = use duration (person-days) as the weight in S-curve analytics.\neffort::Float64: how much work the task is, in whatever unit is also used for Person's capacity (hours, person-days, points — Perth never needs to know which, only that the two agree). 0 = fall back to cost, and then to the duration in person-days, which is what daily load has always been measured in. It never moves the task: two hours of work inside a task that spans a week is a statement about load, not about dates. See workload and overallocations.\ncolor::String: hex color (e.g. \"#bd93f9\"); empty string means automatic.\nassignee::String: person or resource responsible.\nnotes::String: free-form notes.\nmilestone::Bool: render as a diamond marker instead of a bar.\nparent::String: id of the parent task (WBS). A task with children is a summary: its start, duration and progress are derived from its descendants on every save (see set_parent!).\norder::Int: position among its siblings, 1-based. 0 (the default) means no manual position: the task falls back to the date ordering, after the ones that were placed by hand. Set by dragging a row in the UI or by move_task! — see ordered_tasks.\nbaseline_start::Union{Nothing,Date} / baseline_duration::Int: snapshot taken by set_baseline!; nothing/0 = no baseline.\ndeadline::Union{Nothing,Date}: a date the task must not finish after. It never moves the task — it caps the late finish in the CPM backward pass, so busting it turns the slack of this task and of everything feeding it negative. nothing = no commitment.\nstatus::String: a state only a person can know, from a closed vocabulary — \"\" (the normal case) or \"hold\", work stopped and expected to resume. Every other state Perth shows (not started, in progress, done, overdue, slipped, past deadline, pinned, overallocated, bottleneck) is derived from dates, progress and the dependency graph, and stays true on its own; this one cannot be derived, because nothing in the plan says the permit is late. Closed on purpose: free text becomes fifteen spellings of \"parado\" and nothing can filter on it. Unknown values normalise to \"\".\nIt does not change any arithmetic. A task on hold is still planned work: it keeps its dates, its load, its place in the critical path. What it stops is the reader's assumption that the bar being there means somebody is on it.\npinned::Bool: the start date is fixed (a contract date, a delivery window). schedule! leaves it where it is; the engine still computes where it would go, so a pin the plan can no longer honour shows up as an early_start later than start in slack instead of the task silently moving.\noptimistic::Int / most_likely::Int / pessimistic::Int: the PERT three-point estimate, in the same days as duration. All three 0 (the default) means no estimate; see set_estimate! and pert. They never move the task on their own — the expected duration (o + 4m + p)/6 only reaches duration through pert!, the same way schedule! is what moves dates.\n\n\n\n\n\n","title":"Perth.GanttTask"},{"category":"type","location":"gantt/#Perth.Project","page":"Gantt","text":"Project(; name, kwargs...)\n\nA project: a named collection of GanttTasks.\n\n\n\n\n\n","title":"Perth.Project"},{"category":"type","location":"gantt/#Perth.Person","page":"Gantt","text":"Person(; name, role = \"\", team = \"\", email = \"\", notes = \"\", capacity = 0)\n\nA registered collaborator. name is the only field that matters to the schedule — it is what a task's assignee holds. The rest is the address book around it: who they are, so a name in a plan does not need a side channel to be understood.\n\nFields\n\nname::String: as it appears in assignee. Registering a name is what fixes its spelling everywhere (see people!).\nrole::String: job title (\"Arquiteta\", \"Eletricista\").\nteam::String: department, squad, company — whatever the org divides by.\nemail::String: free text; nothing sends mail on its own.\nnotes::String: anything else (phone, shift, holidays).\ncapacity::Float64: how much work this person absorbs on one working day, in the same unit as a task's effort — 8 if effort is in hours, 1 for one full-time person-day, 0.5 for half time. It is what turns \"two tasks on the same day\" into \"more work than the day holds\": two one-hour tasks stop being an overload the moment there is a number to compare them to. See workload and overallocations.\n0 (the default) means not declared, and overallocation falls back to the older, cruder rule — two tasks on the same day. Declaring capacity is therefore opt-in, one person at a time: nobody's plan changes meaning because a field appeared.\n\n\n\n\n\n","title":"Perth.Person"},{"category":"function","location":"gantt/#Perth.run","page":"Gantt","text":"Perth.run(; port = 8123, open_browser = true, data_dir = nothing,\n          share = false, host = nothing, key = \"\", view_key = \"\") -> String\n\nStart the Perth server and (optionally) open the app in your browser. Returns the URL. The server does not block the REPL; stop it with Perth.stop.\n\nBy default only this machine can open the app. Pass share = true to let other machines on the local network open the same projects: every connected machine shows up as a labelled cursor with its name and IP address — exactly like Perth.kanban(share = true). key requires an access key from those machines; view_key adds a second link that opens the projects and refuses to change them — see Perth.view_key!.\n\nSharing is a live switch, not a startup-only decision: turn it on and off with the server running via Perth.share! or the UI (File → Share / QR…). To that end the socket binds to 0.0.0.0 and every connection is checked against the current setting — with sharing off, requests from other machines are refused with 403. Pass host to bind a fixed address instead (which disables the live switch).\n\nIf port is busy, the next free port is used (up to 20 attempts). data_dir overrides the project storage directory (default: $PERTH_DATA_DIR or ~/.perth).\n\nwarning: Warning\nWith share = true and no key, anyone on the local network who knows the port can edit the projects. Never expose the port to the internet.\n\n\n\n\n\n","title":"Perth.run"},{"category":"function","location":"gantt/#Perth.stop","page":"Gantt","text":"Perth.stop()\n\nStop the running Perth server, if any.\n\n\n\n\n\n","title":"Perth.stop"},{"category":"function","location":"gantt/#Perth.check_update","page":"Gantt","text":"Perth.check_update(io = stdout) -> Union{VersionNumber,Nothing}\n\nCheck right now whether a newer Perth has been published, and say so.\n\nThe answer comes from the package registries already installed on this machine — the same ones Pkg resolves against. Nothing is sent over the network, so the reading is only as fresh as the last ] up: a release published minutes ago shows up here once the registry is refreshed.\n\nReturns the newer version, or nothing when Perth is current (or when no registry could be read, which is reported but not raised). The result also updates the cache behind the one-line notice that using Perth prints, so calling this makes the next session's notice accurate.\n\nPERTH_UPDATE_CHECK=0 turns off the notice and the background check; this function still answers whenever you call it directly.\n\njulia> Perth.check_update()\nperth v0.12.0 → 0.13.0 available · ] up Perth\nv\"0.13.0\"\n\n\n\n\n\n","title":"Perth.check_update"},{"category":"function","location":"gantt/#Perth.share!","page":"Gantt","text":"Perth.share!(on = true) -> Bool\n\nTurn network sharing on or off on the running gantt server, live — no restart, no Perth.stop. With sharing on, other machines on the local network can open the same projects (see Perth.run); with it off, only this machine can, and remote browsers already connected are disconnected immediately.\n\nThe same switch is in the UI (File → Share / QR…), available only from the machine running the server. Sharing can only be toggled when the server was started without an explicit host — see Perth.run.\n\n\n\n\n\n","title":"Perth.share!"},{"category":"function","location":"gantt/#Perth.key!","page":"Gantt","text":"Perth.key!(key = \"\") -> Bool\n\nSet (or drop, with \"\") the access key of the running gantt server, live — no restart, no Perth.stop. Returns whether a key is required from now on. Surrounding whitespace is trimmed, so a pasted key does not fail on a trailing space.\n\nMachines on the network must send the key on every request and on the presence socket; the machine running the server never needs it. The LAN links (and the QR code) carry the key, so nobody has to type it — see Perth.run.\n\nChanging the key disconnects every machine on the network immediately: the key they hold is now the wrong one, and each is asked for the new one on screen rather than left with a dead page. Dropping the key (key!()) disconnects nobody — nothing they hold became invalid.\n\nThe same control is in the UI (File → Share / QR…), available only from the machine running the server. For a link that opens the projects but refuses to change them, see Perth.view_key!.\n\nPerth.key!(\"obra-2026\")   # exige a chave de quem vem da rede\nPerth.key!()              # volta a aceitar qualquer um da rede\n\n\n\n\n\n","title":"Perth.key!"},{"category":"function","location":"gantt/#Perth.view_key!","page":"Gantt","text":"Perth.view_key!(key = \"\") -> Bool\n\nSet (or drop, with \"\") the read-only key of the running gantt server, live — no restart, no Perth.stop. Returns whether a read-only link exists from now on.\n\nWhoever opens a link carrying this key sees the projects — chart, table, analytics, exports — and cannot change them: every write is refused with 403, including the ones that would go through the presence socket (chat). That is the link you hand to a client, a director, the whole site.\n\nIt is a second key, independent of Perth.key!: with an access key set, one link edits and the other only shows; with no access key, the plain link still edits and only the read-only link is restricted. The two keys cannot be the same string — one link cannot mean both things.\n\nChanging or dropping the read-only key disconnects the machines that came in through it, and only those: what the editors hold is still valid.\n\nThe machine running the server always edits, even through the read-only link — it is the machine that hands the link out (same reason it never needs the access key). The read-only links therefore start at the network addresses, and the UI (File → Share / QR…) shows them there.\n\nPerth.run(share = true, key = \"obra-2026\", view_key = \"obra-2026-ver\")\nPerth.view_key!(\"so-olhar\")   # troca o link de leitura\nPerth.view_key!()             # acaba com o link de leitura\n\n\n\n\n\n","title":"Perth.view_key!"},{"category":"function","location":"gantt/#Perth.background!","page":"Gantt","text":"Perth.background!(path; opacity = 0.18, interval = 60) -> String\nPerth.background!(dir; …) -> Vector{String}\nPerth.background!(paths::AbstractVector; …) -> Vector{String}\nPerth.background!(; opacity, interval) -> Float64\n\nUse local images as the background of the Perth UI — gantt and kanban, every connected browser. They are read from the machine running the server; the files themselves are never copied, so replacing one on disk changes the background on the next reload.\n\nPoint at one file, at a list of files, or at a directory: a directory is read once, right here, and the images in it (sorted by name, up to 60) become the rotation. Whatever is in there that is not a usable image is skipped, and the log line says how many were taken and how many were left out. The directory is not re-read later — the list is frozen at this call, so a file dropped in the folder afterwards is not published to your network behind your back. Add photos by calling background! on the folder again.\n\nWith more than one image the UI cycles through them, interval seconds apart, fading the current one out and the next one in. Every connected browser derives the current image from the wall clock, so all of them show the same photo without the server ticking. interval = 0 stops the rotation on the first image.\n\nopacity (0–1) is how strongly the image shows through the page colour. The default is deliberately low: panels, cards and bars keep their solid surfaces, and the image fills the space around them. Pass opacity or interval alone to adjust them without changing the images.\n\nThe setting is persisted in settings.json in the Perth data directory. Clear it with Perth.background_clear!; read it back with Perth.background / Perth.backgrounds.\n\nAccepted formats are PNG, JPEG, GIF, WebP and AVIF, checked by content rather than by file extension, up to 12 MB each.\n\nnote: Note\nEvery machine that can open Perth can see these images — they are served over the same port, behind the same access key as the rest of the data. Each browser can hide them locally (settings panel → Hide background), which is a rendering preference, not a way to keep an image private. Point at a folder you would be comfortable showing on the office wall.\n\nPerth.background!(\"~/Imagens/escritorio.jpg\")\nPerth.background!(\"~/Imagens/fundos/\"; interval = 90)\nPerth.background!([\"~/a.jpg\", \"~/b.png\"])\nPerth.background!(opacity = 0.35)\nPerth.background_clear!()\n\n\n\n\n\n","title":"Perth.background!"},{"category":"function","location":"gantt/#Perth.background","page":"Gantt","text":"Perth.background() -> Union{String,Nothing}\n\nPath of the first image used as the UI background, or nothing when none is set (or the files are gone). With a rotation, this is where it starts; Perth.backgrounds gives the whole list. See Perth.background!.\n\n\n\n\n\n","title":"Perth.background"},{"category":"function","location":"gantt/#Perth.backgrounds","page":"Gantt","text":"Perth.backgrounds() -> Vector{String}\n\nEvery image in the UI background rotation, in the order it cycles through them — the list frozen by Perth.background!, minus whatever has since been deleted from disk. Empty when no background is set.\n\n\n\n\n\n","title":"Perth.backgrounds"},{"category":"function","location":"gantt/#Perth.background_clear!","page":"Gantt","text":"Perth.background_clear!()\n\nDrop the UI background, live on every connected browser. The image files themselves are left alone. See Perth.background!.\n\n\n\n\n\n","title":"Perth.background_clear!"},{"category":"function","location":"gantt/#Perth.create_project","page":"Gantt","text":"create_project(name::AbstractString) -> Project\n\nCreate (and persist) a new empty project.\n\n\n\n\n\n","title":"Perth.create_project"},{"category":"function","location":"gantt/#Perth.add_task!","page":"Gantt","text":"add_task!(p::Project, name; start = today(), duration = 1, kwargs...) -> GanttTask\n\nAdd a task to p and persist. Keyword arguments are forwarded to GanttTask (progress, dependencies, color, assignee, notes, milestone).\n\n\n\n\n\n","title":"Perth.add_task!"},{"category":"function","location":"gantt/#Perth.update_task!","page":"Gantt","text":"update_task!(p::Project, id::AbstractString; kwargs...) -> GanttTask\n\nUpdate fields of the task with the given id (any GanttTask field except id) and persist.\n\n\n\n\n\n","title":"Perth.update_task!"},{"category":"function","location":"gantt/#Perth.schedule!","page":"Gantt","text":"schedule!(p::Project) -> Project\n\nReschedule the project so that no task starts before all of its dependencies have finished. Each task's own start date acts as a start-no-earlier-than constraint: tasks are only pushed forward, never pulled back. Tasks marked pinned keep their start date — the engine still computes where they would have to go, so a pin the plan can no longer honour leaves slack's early_start later than the task's start, instead of the date silently moving. Persists the result.\n\nThrows ArgumentError if the dependency graph has a cycle.\n\n\n\n\n\n","title":"Perth.schedule!"},{"category":"function","location":"gantt/#Perth.level!","page":"Gantt","text":"level!(p::Project) -> Project\n\nResource-level the plan: push work forward until nobody with a declared capacity is left with an overloaded day. Dependencies still bind, and successors follow what moves. Persists the result.\n\nThe priority rule is least slack first: the task with the least slack keeps its place and the one with the most slack gives way. Levelling is NP-hard, so this is a heuristic and not the optimum — it is the most defensible one available here, because slack is a number the CPM already computes, and because a deadline comes out protected without a second rule: the backward pass already gives a task with a tight deadline low (or negative) slack, so it is among the last to be asked to move.\n\nThree things never move:\n\ntasks marked pinned — a date somebody promised;\nmilestones, which carry no work to redistribute: moving one would only change a date somebody reads;\na task whose own daily share is heavier than the assignee's whole capacity. It overloads any day it lands on, so pushing it would just walk the problem down the calendar.\n\nEverything else is only ever pushed later, and to the first day where it fits whole — never pulled back, never split in two. Tasks with no assignee are never moved: there is nobody to overload.\n\nLevelling needs a declared capacity, and only touches people who have one. Without a capacity an \"overloaded day\" is just two tasks on the same day (see workload), and levelling against that rule would serialise the whole plan into single file — so people with no declared capacity are left exactly where they are, and a plan where nobody declared one throws ArgumentError instead of quietly rearranging itself.\n\nWhat is left over stays visible. A plan with more work than capacity cannot be levelled, and this does not pretend otherwise: when it returns, any day still over in workload is a day whose remaining work cannot move (the three cases above), or a day belonging to somebody who never declared a capacity. A day that stays overloaded is still emptied of everything that could leave it — the day is beyond help, the tasks that can find room are not. Ask workload or people_stats again afterwards; the overload that survives is the answer, in the same numbers that reported it before.\n\nThrows ArgumentError if the dependency graph has a cycle, or if no assignee in the plan has a declared capacity.\n\nExample\n\nadd_person!(p, \"Ana\"; capacity = 8)      # 8 hours a day\nupdate_task!(p, \"t1\"; assignee = \"Ana\", effort = 8)\nupdate_task!(p, \"t2\"; assignee = \"Ana\", effort = 8)   # same day: 16 in an 8\nlevel!(p)                                # t2 moves to the first day that fits\nfilter(r -> r.over, workload(p))         # what levelling could not solve\n\n\n\n\n\n","title":"Perth.level!"},{"category":"function","location":"gantt/#Perth.critical_path","page":"Gantt","text":"critical_path(p::Project) -> Vector{String}\n\nIds of the tasks on the critical path (slack ≤ 0), in topological order. Delaying any of these delays the whole project.\n\nSlack is only ever negative when a deadline is already missed, and those tasks are more critical than the zero-slack ones — leaving them out would hide exactly the chain that is late.\n\n\n\n\n\n","title":"Perth.critical_path"},{"category":"function","location":"gantt/#Perth.slack","page":"Gantt","text":"slack(p::Project) -> Vector{NamedTuple}\n\nPer-task CPM summary as Tables.jl-compatible rows: id, name, early_start, early_finish, slack_days, critical, dependents, bottleneck.\n\nslack_days goes negative when a deadline cannot be met: the task and everything feeding it are late by that many days. critical is slack_days ≤ 0.\n\ndependents counts how many other leaf tasks name this one as a predecessor — the fan-out, not the whole downstream. bottleneck is critical && dependents ≥ 2: a zero-slack task that more than one thing is waiting on. A critical task with a single dependent is just a link in the chain, which critical already says; the bottleneck is where the chain becomes a funnel, and it is the task worth protecting first.\n\nBoth are derived, never declared. A hand-typed \"bottleneck\" flag would be wrong the moment somebody drags a bar, and nobody comes back to fix it.\n\n\n\n\n\n","title":"Perth.slack"},{"category":"function","location":"gantt/#Perth.deadline_slip","page":"Gantt","text":"deadline_slip(p::Project) -> Vector{NamedTuple}\n\nThe tasks whose planned finish is past their deadline, as Tables.jl rows: id, name, deadline, finish (calendar-aware) and slip_days — calendar days late, like slippage, not the business days the CPM slack is measured in.\n\nTasks without a deadline, and deadlines still being met, produce no row; an empty result means every commitment in the plan holds.\n\n\n\n\n\n","title":"Perth.deadline_slip"},{"category":"function","location":"gantt/#Perth.set_baseline!","page":"Gantt","text":"set_baseline!(p::Project) -> Project\n\nSnapshot the current plan: every task's start/duration is copied to its baseline_start/baseline_duration, and p.baseline_at records when. The web UI then draws the baseline as ghost bars and flags slipped tasks; slippage reports the deviation. Persists.\n\n\n\n\n\n","title":"Perth.set_baseline!"},{"category":"function","location":"gantt/#Perth.clear_baseline!","page":"Gantt","text":"clear_baseline!(p::Project) -> Project\n\nRemove the baseline snapshot from every task and from the project. Persists.\n\n\n\n\n\n","title":"Perth.clear_baseline!"},{"category":"function","location":"gantt/#Perth.has_baseline","page":"Gantt","text":"has_baseline(t::GanttTask) -> Bool\n\nWhether the task carries a baseline snapshot.\n\n\n\n\n\n","title":"Perth.has_baseline"},{"category":"function","location":"gantt/#Perth.slippage","page":"Gantt","text":"slippage(p::Project) -> Vector{NamedTuple}\n\nTables.jl-compatible rows comparing the current plan against the baseline, for every task that has one: id, name, baseline_start, baseline_finish, start, finish and slip_days (positive = the task now ends later than planned; calendar days).\n\n\n\n\n\nslippage(p::Project, id::AbstractString) -> Int\n\nSlip of one task in calendar days (positive = later than the baseline). Throws if the task has no baseline.\n\n\n\n\n\n","title":"Perth.slippage"},{"category":"function","location":"gantt/#Perth.set_estimate!","page":"Gantt","text":"set_estimate!(p::Project, id, optimistic, most_likely, pessimistic;\n              apply = true) -> GanttTask\n\nGive a task its PERT three-point estimate, in the same days as duration. With apply = true (the default) the task's duration becomes the expected duration (o + 4m + p)/6, rounded to whole days (a tie rounds up: 4.5 becomes 5) — pass apply = false to record the estimate without touching the plan. Persists.\n\nThe three numbers are pushed into order rather than sorted: the optimistic estimate is a floor and the pessimistic one a ceiling, so set_estimate!(p, id, 8, 5, 6) records 8, 8, 8 instead of silently swapping the fields you filled in. A partial estimate is allowed — pass 0 for what you don't know and the current duration fills the gap.\n\nset_estimate!(p, t.id, 4, 6, 14)   # duration becomes 7\npert(p)                            # the whole table, with σ per task\n\n\n\n\n\n","title":"Perth.set_estimate!"},{"category":"function","location":"gantt/#Perth.clear_estimate!","page":"Gantt","text":"clear_estimate!(p::Project, id) -> GanttTask\n\nDrop a task's three-point estimate. The duration it produced stays — removing the estimate is not a reason to move the plan. Persists.\n\n\n\n\n\n","title":"Perth.clear_estimate!"},{"category":"function","location":"gantt/#Perth.has_estimate","page":"Gantt","text":"has_estimate(t::GanttTask) -> Bool\n\nWhether the task carries a PERT three-point estimate.\n\n\n\n\n\n","title":"Perth.has_estimate"},{"category":"function","location":"gantt/#Perth.expected_duration","page":"Gantt","text":"expected_duration(t::GanttTask) -> Float64\n\nThe PERT expected duration (o + 4m + p)/6, in the same days as duration. Falls back to the task's plain duration when it has no estimate, so it is safe to call on any task.\n\n\n\n\n\n","title":"Perth.expected_duration"},{"category":"function","location":"gantt/#Perth.pert","page":"Gantt","text":"pert(p::Project) -> Vector{NamedTuple}\n\nThe project's three-point estimates as Tables.jl-compatible rows, in WBS display order, one per estimated task: id, name, optimistic, most_likely, pessimistic, expected (te, unrounded), sd (σ), variance and duration (what the plan currently uses — it differs from expected until pert! runs).\n\nTasks without an estimate produce no row, so an empty result means nothing in the plan has been estimated yet.\n\n\n\n\n\n","title":"Perth.pert"},{"category":"function","location":"gantt/#Perth.pert!","page":"Gantt","text":"pert!(p::Project) -> Project\n\nApply every three-point estimate on the project: each estimated task's duration becomes its expected duration (o + 4m + p)/6, rounded to whole days (a tie rounds up, so te = 4.5 schedules 5 days — never 4). Milestones and tasks without an estimate are left alone; WBS summaries derive their span from their children as always. Persists.\n\nThis is the only thing that turns estimates into a plan — deliberately, like schedule! being the only thing that moves dates. Estimate first, pert!, then schedule! to push the successors around the new durations.\n\n\n\n\n\n","title":"Perth.pert!"},{"category":"function","location":"gantt/#Perth.pert_finish","page":"Gantt","text":"pert_finish(p::Project) -> NamedTuple\n\nThe probabilistic finish of the project under its three-point estimates: expected (the CPM finish computed with expected durations), sd_days (σ of that finish), variance, critical (how many tasks are on the critical path) and estimated (how many of those carry an estimate).\n\nNothing is written: the estimates feed the engine directly, so the answer is the same whether or not pert! has run.\n\nσ is the textbook one — the square root of the variance summed along the critical path (the riskiest one, when several tie at zero slack). It is an estimate of the spread, not a forecast of the date: parallel chains that are nearly critical can become critical when they slip, and this formula cannot see them, so its expected is optimistic whenever the project has several fronts merging (the classic merge bias). When that matters, ask pert_simulate instead, which reschedules the whole project thousands of times and does see them.\n\n\n\n\n\n","title":"Perth.pert_finish"},{"category":"function","location":"gantt/#Perth.finish_probability","page":"Gantt","text":"finish_probability(p::Project, date::Date) -> Float64\n\nProbability (0–1) that the project finishes on or before date, from the PERT estimates: Φ((date − expected) / σ).\n\nWith no uncertainty on the critical path (σ = 0 — nothing estimated, or every estimate a single point) the answer is the certainty of the plan itself: 1.0 from the expected finish onwards, 0.0 before it.\n\nSee pert_finish for what σ does and does not cover.\n\n\n\n\n\n","title":"Perth.finish_probability"},{"category":"function","location":"gantt/#Perth.pert_date","page":"Gantt","text":"pert_date(p::Project, probability::Real) -> Date\n\nThe date the project finishes by with the given confidence — the P80 a sponsor asks for when the expected finish (P50) is not a promise anyone wants to make.\n\nInverse of finish_probability: expected + z·σ, rounded to whole days and shifted through the project's calendar, so under a business-day calendar the buffer is counted in working days too.\n\n\n\n\n\n","title":"Perth.pert_date"},{"category":"function","location":"gantt/#Perth.pert_simulate","page":"Gantt","text":"pert_simulate(p::Project; n = 10_000, rng = Random.default_rng()) -> NamedTuple\n\nMonte Carlo over the project's three-point estimates: draw a duration for every estimated task from its Beta-PERT distribution, re-run the CPM forward pass, and repeat n times. Returns runs, mean_days and sd_days (spread of the simulated finishes, in days around the mean), expected (the mean finish as a date) and the p10, p50, p80 and p90 finish dates.\n\nThis is pert_finish without its blind spot. The formula only propagates variance along today's critical path; here every path is sampled, so a parallel chain with one day of slack and a wide estimate shows up as the risk it is. Expect p50 at or later than the formula's expected finish, and the gap to grow with the number of near-critical chains — that gap is the merge bias, made visible.\n\nPass a seeded rng (Random.MersenneTwister(1)) for a reproducible run. Cost is one CPM pass per draw: a few hundred tasks × 10 000 draws is a fraction of a second.\n\n\n\n\n\n","title":"Perth.pert_simulate"},{"category":"function","location":"gantt/#Perth.set_calendar!","page":"Gantt","text":"set_calendar!(p::Project, name::AbstractString) -> Project\n\nSet the project's working-day calendar (a BusinessDays.jl calendar name, e.g. \"Brazil\", \"BRSettlement\", \"USSettlement\", \"WeekendsOnly\"). Durations are then interpreted as business days by the scheduling engine. Pass \"\" to revert to calendar days. Requires using BusinessDays for scheduling to run. Persists the change.\n\n\n\n\n\n","title":"Perth.set_calendar!"},{"category":"function","location":"gantt/#Perth.set_parent!","page":"Gantt","text":"set_parent!(p::Project, id, parent) -> GanttTask\n\nMove the task id under parent in the WBS (pass nothing or \"\" to promote it to top level). Throws ArgumentError when the parent is a milestone or when the move would create a cycle. Persists — the parent becomes a summary and rolls up on save.\n\n\n\n\n\n","title":"Perth.set_parent!"},{"category":"function","location":"gantt/#Perth.move_task!","page":"Gantt","text":"move_task!(p::Project, id; parent = missing, position = nothing) -> GanttTask\n\nPut the task id where you want it in the WBS: parent changes whose child it is (nothing or \"\" promotes it to top level; missing, the default, leaves it where it is) and position is its 1-based place among its siblings (nothing keeps it last). This is what dragging a row up or down in the web UI does.\n\nPositioning renumbers the whole sibling group, so the result is always a clean 1, 2, 3, …: no gaps to run out of, and the group can never end up half by hand and half by date. Siblings elsewhere are untouched — a manual order is a statement about one group, not about the plan.\n\nThrows the same ArgumentErrors as set_parent! (a milestone cannot have subtasks; a task cannot move under its own descendant).\n\nmove_task!(p, \"a3f\"; position = 1)              # primeira do grupo\nmove_task!(p, \"a3f\"; parent = \"b12\")            # vira subtarefa de b12\nmove_task!(p, \"a3f\"; parent = \"\", position = 2) # sobe ao topo, em 2º\n\n\n\n\n\n","title":"Perth.move_task!"},{"category":"function","location":"gantt/#Perth.ordered_tasks","page":"Gantt","text":"ordered_tasks(p::Project) -> Vector{Tuple{GanttTask,Int}}\n\nTasks in WBS display order — depth-first, children under their parent — paired with their depth (0 = top level). This is the row order used by the web UI and the Makie figure.\n\nSiblings come out in the order someone put them in (order, see move_task!); the ones with no manual position come after, by (start, name). A plan nobody reordered is therefore ordered by date, exactly as before the field existed.\n\n\n\n\n\n","title":"Perth.ordered_tasks"},{"category":"function","location":"gantt/#Perth.people","page":"Gantt","text":"people(p::Project) -> Vector{Person}\n\nRegistered collaborators, alphabetically. This is the project vocabulary: it feeds the assignee autocomplete, fixes the spelling of a name everywhere, and gives a name (with role, team…) to people who have no task yet.\n\nThe assignee field stays free text — the list is convenience, not a fence, and names used by tasks are offered by the UI even when not registered.\n\n\n\n\n\n","title":"Perth.people"},{"category":"function","location":"gantt/#Perth.person","page":"Gantt","text":"person(p::Project, name) -> Union{Person,Nothing}\n\nThe registered collaborator with this name (case-insensitive), or nothing.\n\n\n\n\n\n","title":"Perth.person"},{"category":"function","location":"gantt/#Perth.people!","page":"Gantt","text":"people!(p::Project, entries) -> Vector{Person}\n\nReplace the collaborator list. Each entry may be a name, a Person, or a NamedTuple/Dict of fields. Names are trimmed, de-duplicated (ignoring case) and sorted; task assignees are re-spelled to match a registered name that differs only in case — which is how you fix a spelling across the whole project.\n\npeople!(p, [\"Ana Paula\", \"Bruno\"])\npeople!(p, [(name = \"Ana Paula\", role = \"Arquiteta\", team = \"Projetos\"),\n            (name = \"Bruno\", role = \"Eletricista\")])\n\n\n\n\n\n","title":"Perth.people!"},{"category":"function","location":"gantt/#Perth.add_person!","page":"Gantt","text":"add_person!(p::Project, name; role = \"\", team = \"\", email = \"\", notes = \"\")\n\nRegister one collaborator. A name already on the list (in any case) is updated, not duplicated: the spelling you pass wins, and only the keyword fields you pass are overwritten — so add_person!(p, \"Ana\"; team = \"Obra\") does not erase her role.\n\n\n\n\n\n","title":"Perth.add_person!"},{"category":"function","location":"gantt/#Perth.remove_person!","page":"Gantt","text":"remove_person!(p::Project, name) -> Vector{Person}\n\nUnregister a collaborator. Tasks assigned to them keep the name — removing someone from the list must not silently orphan their work.\n\n\n\n\n\n","title":"Perth.remove_person!"},{"category":"type","location":"gantt/#Perth.Band","page":"Gantt","text":"Band(; name, from, to, color = \"\")\n\nA named stretch of calendar shaded behind the chart: a sprint, a shutdown, a rainy season, the two weeks when the crane is on site. It is annotation, not scheduling — a period never moves a task, constrains a date or enters the CPM engine. It answers \"why is this stretch different?\", which until now had to live in someone's head or in a note nobody opens.\n\nFields\n\nname::String: written along the band, on its left edge.\nfrom::Date / to::Date: inclusive on both ends — a period of one day has from == to. Inverted ranges are swapped on save, the way a negative duration is clamped: it is a typo, not a plan.\ncolor::String: hex tint (e.g. \"#7cc4a4\"); empty picks one automatically.\n\nBands may overlap — a crunch week inside a sprint is a real thing to say.\n\n\n\n\n\n","title":"Perth.Band"},{"category":"function","location":"gantt/#Perth.bands","page":"Gantt","text":"bands(p::Project) -> Vector{Band}\n\nNamed calendar bands, earliest first. See Band.\n\n\n\n\n\n","title":"Perth.bands"},{"category":"function","location":"gantt/#Perth.bands!","page":"Gantt","text":"bands!(p::Project, entries) -> Vector{Band}\n\nReplace the band list. Entries may be Bands or NamedTuples/Dicts of fields; nameless bands are dropped (a shaded stretch that does not say why is noise) and inverted ranges are swapped.\n\n\n\n\n\n","title":"Perth.bands!"},{"category":"function","location":"gantt/#Perth.add_band!","page":"Gantt","text":"add_band!(p::Project, name, from, to; color = \"\") -> Vector{Band}\n\nShade from–to (both inclusive) and label it name.\n\nadd_band!(p, \"Sprint 1\", Date(2026, 3, 2), Date(2026, 3, 27))\nadd_band!(p, \"Chuvas\", Date(2026, 1, 1), Date(2026, 3, 31); color = \"#6fa8dc\")\n\nA name that already exists is moved, not duplicated: two bands with the same label on different weeks is almost always a second attempt, not a plan.\n\n\n\n\n\n","title":"Perth.add_band!"},{"category":"function","location":"gantt/#Perth.remove_band!","page":"Gantt","text":"remove_band!(p::Project, name) -> Vector{Band}\n\nRemove the band with this name (case-insensitive).\n\n\n\n\n\n","title":"Perth.remove_band!"},{"category":"type","location":"gantt/#Perth.Marker","page":"Gantt","text":"Marker(; name, date, color = \"\")\n\nA named day, drawn as a vertical line across the chart — the way the today line is drawn, and for the same reason: some dates matter to every task at once. A delivery, an audit, the day the scaffolding comes down.\n\nLike Band it is annotation: a marker never moves a task and never enters the CPM engine. When a date must actually bind a task, that is a deadline on the task, which does change its slack.\n\nFields\n\nname::String: written along the line.\ndate::Date: the day the line falls on.\ncolor::String: hex tint; empty picks one automatically.\nlabel_at::Int: how far down the chart the name is written, 0–100 percent of the chart height (0, the default, is the top). The name lies along the line, so wherever it sits it may land on top of a bar — this is what moves it out of the way. A percentage rather than pixels: the chart grows with the plan, and \"a third of the way down\" should stay a third of the way down.\n\n\n\n\n\n","title":"Perth.Marker"},{"category":"function","location":"gantt/#Perth.markers","page":"Gantt","text":"markers(p::Project) -> Vector{Marker}\n\nNamed days, earliest first. See Marker.\n\n\n\n\n\n","title":"Perth.markers"},{"category":"function","location":"gantt/#Perth.markers!","page":"Gantt","text":"markers!(p::Project, entries) -> Vector{Marker}\n\nReplace the marker list. Nameless markers are dropped: a line that does not say what it marks is just a stroke on the screen.\n\n\n\n\n\n","title":"Perth.markers!"},{"category":"function","location":"gantt/#Perth.add_marker!","page":"Gantt","text":"add_marker!(p::Project, name, date; color = \"\", label_at = 0) -> Vector{Marker}\n\nDraw a vertical line on date, labelled name. label_at is how far down the chart the name is written, 0–100 percent (see Marker); the slider in File → Marked days… is the same setting.\n\nadd_marker!(p, \"Entrega\", Date(2026, 4, 30))\nadd_marker!(p, \"Vistoria\", Date(2026, 5, 8); label_at = 60)  # nome mais abaixo\n\nA name that already exists is moved to the new date rather than duplicated.\n\n\n\n\n\n","title":"Perth.add_marker!"},{"category":"function","location":"gantt/#Perth.remove_marker!","page":"Gantt","text":"remove_marker!(p::Project, name) -> Vector{Marker}\n\nRemove the marker with this name (case-insensitive).\n\n\n\n\n\n","title":"Perth.remove_marker!"},{"category":"type","location":"gantt/#Perth.MonthMark","page":"Gantt","text":"MonthMark(; month, name = \"\", color = \"\")\n\nA whole month painted in the ruler at the top of the chart. It is the month-sized companion of Marker: where a marked day draws a line across the plan, a marked month colours the strip that names it — \"this is the month of the shutdown\", said once, at the top, instead of repeated on every task inside it.\n\nUnlike Band it does not touch the body of the chart. A band shades the work behind it and answers \"why is this stretch different?\"; a marked month is a label on the calendar itself, and leaves the bars alone. Both are annotation: neither moves a task or enters the CPM engine.\n\nFields\n\nmonth::Date: any day of the month; it is normalised to the first.\nname::String: shown next to the month in the ruler when it fits, and in the tooltip either way. Optional — the month already writes its own name, so the colour alone is allowed to be the whole message.\ncolor::String: hex tint; empty picks one automatically.\n\n\n\n\n\n","title":"Perth.MonthMark"},{"category":"function","location":"gantt/#Perth.month_marks","page":"Gantt","text":"month_marks(p::Project) -> Vector{MonthMark}\n\nThe months painted in the ruler at the top of the chart (see MonthMark), in date order.\n\n\n\n\n\n","title":"Perth.month_marks"},{"category":"function","location":"gantt/#Perth.month_marks!","page":"Gantt","text":"month_marks!(p::Project, entries) -> Vector{MonthMark}\n\nReplace the marked months. Accepts MonthMarks or named tuples; each month is normalised to the first day of its month, and a month listed twice keeps the last one.\n\n\n\n\n\n","title":"Perth.month_marks!"},{"category":"function","location":"gantt/#Perth.add_month_mark!","page":"Gantt","text":"add_month_mark!(p::Project, month; name = \"\", color = \"\") -> Vector{MonthMark}\n\nPaint a month in the ruler. month is any day of it.\n\nadd_month_mark!(p, Date(2026, 12); name = \"recesso\", color = \"#cb3c33\")\n\nMarking a month that is already marked replaces it — a month has one colour, and a second entry for the same month would be a correction, not a second month.\n\n\n\n\n\n","title":"Perth.add_month_mark!"},{"category":"function","location":"gantt/#Perth.remove_month_mark!","page":"Gantt","text":"remove_month_mark!(p::Project, month) -> Vector{MonthMark}\n\nUnpaint the month containing month.\n\n\n\n\n\n","title":"Perth.remove_month_mark!"},{"category":"function","location":"gantt/#Perth.tasktable","page":"Gantt","text":"tasktable(p::Project) -> Vector{NamedTuple}\n\nThe project's tasks as Tables.jl-compatible rows in WBS display order — ready for DataFrame(tasktable(p)), CSV.write, etc. Columns: id, name, wbs_depth, parent, summary, start, duration, finish (calendar-aware), deadline, deadline_slip (calendar days past it; missing without a deadline), pinned, progress, assignee, dependencies, color, notes, milestone, baseline_start, baseline_finish, slip_days (missing without a baseline), optimistic, most_likely, pessimistic and expected — the PERT three-point estimate and the duration it implies (all missing on a task without an estimate).\n\n\n\n\n\n","title":"Perth.tasktable"},{"category":"function","location":"gantt/#Perth.add_tasks!","page":"Gantt","text":"add_tasks!(p::Project, table) -> Project\nadd_tasks!(p::Project, csv::AbstractString) -> Project\n\nAppend tasks to p from any Tables.jl source (DataFrame, CSV.File, a vector of NamedTuples, …), or from CSV given as a file path or as the text itself — no CSV package needed, and the reader takes what a spreadsheet actually produces: ; as well as ,, a UTF-8 BOM, quoted fields with separators and line breaks inside them.\n\nRequired column: name. Optional columns: id, start (Date or ISO string), duration, progress, assignee, notes, color, cost, effort, status, milestone, deadline, pinned, optimistic, most_likely, pessimistic (the PERT three-point estimate), parent and dependencies (a vector of ids, or a \";\"/\",\"/space-separated string). Columns Perth does not know are ignored — a real spreadsheet carries columns that belong to whoever wrote it.\n\nparent and dependencies may name a task by id or by name. The CSV Perth exports cites ids; the spreadsheet a person writes cites names, because nobody types a3f81c02 into a cell. Both are resolved — tasks arriving in this table first, then tasks already in the project — so an exported file comes back whole and a hand-made sheet walks in. A reference that matches neither is dropped on save, as it always was.\n\nAn id is honoured when it is free, which is what makes the export round-trip: the same file re-imported into a project that already has those ids gets fresh ones instead, and its internal references follow the new copies rather than pointing back at the old ones.\n\nThe estimate is recorded, not applied: duration stays whatever the table says until pert! runs.\n\n\n\n\n\n","title":"Perth.add_tasks!"},{"category":"function","location":"gantt/#Perth.icalendar","page":"Gantt","text":"icalendar(p::Project) -> String\nicalendar(p::Project, path::AbstractString) -> String\n\nThe project's commitments as an iCalendar (.ics) document: one all-day event per milestone and one per task deadline, ready to import into any calendar app. Ordinary tasks are left out on purpose — a two-week bar is noise in a calendar; a point in time and a promise are not.\n\nMilestone events are named after the task; deadline events are prefixed with Deadline: and carry the planned finish (and how late it is) in the description. Summary tasks are containers, not work, so they are skipped. Events are TRANSP:TRANSPARENT — they do not mark the day busy — and their UIDs are stable, so re-importing an updated file updates the events instead of duplicating them.\n\nWith path, writes the document there and returns the path; otherwise returns the document as a String.\n\n\n\n\n\n","title":"Perth.icalendar"},{"category":"function","location":"gantt/#Perth.overallocations","page":"Gantt","text":"overallocations(p::Project) -> Vector{NamedTuple}\n\nPairs of leaf tasks assigned to the same person that share at least one overloaded day — Tables.jl-compatible rows with assignee, task1, task1_name, task2, task2_name, from and to (the first and last overloaded day the two share). Summaries are containers, not work, so they are ignored.\n\nWhat counts as overloaded is the one definition the whole package uses (see workload): with a capacity declared for the person, a day whose work exceeds it; without one, a day carrying two or more tasks. So declaring capacity = 8 and two one-hour tasks makes the pair stop being reported — which is the entire point of declaring it.\n\nOnly working days count. Under a business-day calendar (set_calendar!) two tasks that merely touch across a weekend never share a day of work, and are not a conflict.\n\n\n\n\n\n","title":"Perth.overallocations"},{"category":"function","location":"gantt/#Perth.workload","page":"Gantt","text":"workload(p::Project) -> Vector{NamedTuple}\n\nDaily load per person: one Tables.jl-compatible row for each (assignee, date) pair that has work on it, with tasks (how many run that day), effort (the day's share of the tasks' weight — cost when set, otherwise person-days, as in the S-curve) and task_ids. Rows are sorted by person, then date.\n\nOnly leaf tasks with an assignee count — summaries are containers, not work — and only working days: under a business-day calendar (set_calendar!) a holiday carries no load. Days with no work produce no row, so the result stays small on long projects.\n\ntasks ≥ 2 is the same overlap overallocations reports as a pair, seen day by day instead of pair by pair.\n\n\n\n\n\n","title":"Perth.workload"},{"category":"function","location":"gantt/#Perth.people_stats","page":"Gantt","text":"people_stats(p::Project) -> Vector{NamedTuple}\n\nOne Tables.jl-compatible row per person with work in the project: how much they carry, how far along it is, and where it hurts.\n\nColumns\n\nassignee, role, team: the name as the tasks spell it, plus the registry's answer for who they are (empty when not registered — see add_person!).\ntasks, milestones: leaf tasks assigned to them. Summaries are containers, not work, so they are never counted.\neffort, done, progress: total weight (cost when set, otherwise person-days — the same weight the S-curve uses), how much of it is reported complete, and the percentage.\nfirst, last: first and last working day they are occupied.\nbusy_days: distinct days with work. Under a business-day calendar (set_calendar!) holidays carry none.\ncapacity: the work they absorb per working day, from the registry (add_person!); 0 when not declared.\nover_days: days that carry more work than they hold — over capacity when it is declared, two or more of their own tasks when it is not. Same definition overallocations reports pair by pair and the resource panel paints.\nlate: their tasks finishing after their deadline (see deadline_slip).\n\nPeople with no assigned task do not appear, registered or not: this is a table about work, not a staff list. Tasks with no assignee are grouped under the empty name, because unowned work is a fact about the plan, not a gap to hide.\n\n\n\n\n\n","title":"Perth.people_stats"},{"category":"function","location":"gantt/#Perth.team_stats","page":"Gantt","text":"team_stats(p::Project) -> Vector{NamedTuple}\n\nThe same accounting as people_stats, rolled up by the team field of the collaborator registry: one row per team, plus members (how many distinct people) and people (their names).\n\nA team only exists because somebody was registered into it (add_person!); tasks whose assignee has no team — or no registration at all — land in the row with the empty team name.\n\nbusy_days counts days on which anything in the team was running, so it is occupancy of the calendar, not the sum of the members' days. over_days is the sum of the members' overloaded days: two people from the same team working the same day is normal, and only an individual can be double-booked.\n\ncapacity is the sum of the members' declared capacities — two people at 8 absorb 16 in a day. A member who declared none adds zero, so a team where somebody is missing a number reads lower than it is; members sits next to it for exactly that reason.\n\n\n\n\n\n","title":"Perth.team_stats"},{"category":"function","location":"gantt/#Perth.ganttplot","page":"Gantt","text":"ganttplot(p::Project; kwargs...)\n\nRender p as a static Makie figure. Requires a Makie backend (e.g. CairoMakie) to be loaded — the method is provided by the PerthMakieExt package extension.\n\n\n\n\n\n","title":"Perth.ganttplot"},{"category":"function","location":"gantt/#Perth.save_chart","page":"Gantt","text":"save_chart(p::Project, path; kwargs...) -> String\n\nRender p with ganttplot and save the figure to path (.png, .svg, .pdf) at px_per_unit = 2. Requires a Makie backend (e.g. CairoMakie) — provided by the PerthMakieExt package extension.\n\n\n\n\n\n","title":"Perth.save_chart"},{"category":"section","location":"#Perth.jl","page":"Home","text":"Gantt-chart project management with a Julia backend and a local web UI, in the spirit of Pluto.jl — plus a collaborative kanban board with real-time labelled cursors for every machine on your network.\n\nusing Perth\nPerth.run()                 # Gantt at http://localhost:8123\nPerth.kanban(share = true)  # Kanban shared on your LAN, with a QR code","title":"Perth.jl"},{"category":"section","location":"#Highlights","page":"Home","text":"REPL-first: the browser and the REPL edit the same data, live.\nMultiplayer: share = true (Gantt and Kanban) shows every connected machine as a labelled cursor with its name and IP, pair-programming style. Sharing switches on and off live — Perth.share!, kanban_share! or the Share / QR… dialog — without restarting the server, and so does the access key (Perth.key!, kanban_key! or the same dialog). Perth.view_key! adds a second link that opens the projects and refuses to change them — the one you hand to a client.\nReal scheduling: CPM, critical path, slack, dependency lag and SS/FF link types, business-day calendars via BusinessDays.jl.\nResource levelling: level! delays what has slack until nobody is over their declared daily capacity — least slack first, so the critical path and anything with a deadline give way last.\nCommitments vs. plan: a task's deadline never moves it — busting one turns the slack of that task and of everything feeding it negative (deadline_slip); a pinned start is left alone by schedule!.\nGantt ↔ Kanban bridge: kanban_from_project! turns a project into cards; dragging a linked card to done completes the task, live.\nAnalytics: S-curve (planned vs. earned, and the division said out loud — 13% below plan to date, for work and for money), daily load per person (workload and the resource panel), kanban flow metrics, activity log, CSV, iCalendar (icalendar) and PNG/PDF export.\nA spreadsheet walks in: add_tasks! reads CSV from a path or a string — no CSV package needed — and parent/dependencies may name tasks by name, so a hand-written sheet imports as readily as an exported one.\nFive UI languages: English, Português, Español, Français, 中文.","title":"Highlights"},{"category":"section","location":"kanban/#Kanban","page":"Kanban","text":"","title":"Kanban"},{"category":"function","location":"kanban/#Perth.kanban","page":"Kanban","text":"Perth.kanban(; port = 8150, share = false, host = nothing,\n             open_browser = true, data_dir = nothing) -> String\n\nStart the collaborative kanban board and (optionally) open it in your browser. Returns the URL. Stop it with Perth.kanban_stop.\n\nBy default only this machine can open the board, like Perth.run. Pass share = true to let other machines on the local network open the same board: every change (dragging a card, editing, renaming a column) is broadcast live over a WebSocket, and each connected machine shows up as a labelled cursor with its name and IP address — pair-programming style.\n\nSharing is a live switch, not a startup-only decision: turn it on and off with the board running via kanban_share! or the UI (Board → Share / QR…). To that end the socket binds to 0.0.0.0 and every connection is checked against the current setting — with sharing off, requests from other machines are refused with 403. Pass host to bind a fixed address instead (which disables the live switch).\n\nThe board is a single shared entity, persisted as kanban.json in the Perth data directory (data_dir overrides it). The REPL operates on the same data: kanban_add_card!, kanban_move_card!, kanban_remove_card! and kanban_cards — REPL edits appear live in every connected browser.\n\nwarning: Warning\nWith share = true there is no authentication: anyone on the local network who knows the port can edit the board. Never expose the port to the internet.\n\n\n\n\n\n","title":"Perth.kanban"},{"category":"function","location":"kanban/#Perth.kanban_stop","page":"Kanban","text":"Perth.kanban_stop()\n\nStop the running kanban server, if any.\n\n\n\n\n\n","title":"Perth.kanban_stop"},{"category":"function","location":"kanban/#Perth.kanban_share!","page":"Kanban","text":"kanban_share!(on = true) -> Bool\n\nTurn network sharing on or off on the running kanban server, live — no restart, no Perth.kanban_stop. With sharing on, other machines on the local network can open the same board (see Perth.kanban); with it off, only this machine can, and remote browsers already connected are disconnected immediately.\n\nThe same switch is in the UI (Board → Share / QR…), available only from the machine running the server. Sharing can only be toggled when the server was started without an explicit host.\n\n\n\n\n\n","title":"Perth.kanban_share!"},{"category":"function","location":"kanban/#Perth.kanban_key!","page":"Kanban","text":"kanban_key!(key = \"\") -> Bool\n\nSet (or drop, with \"\") the access key of the running kanban board, live — no restart, no Perth.kanban_stop. Returns whether a key is required from now on. Twin of Perth.key! for the board: machines on the network must send the key (the links Perth prints carry it), the machine running the server never needs it, and changing it disconnects everyone on the network, who are then asked for the new key on screen. Dropping the key disconnects nobody.\n\nThe same control is in the UI (Board → Share / QR…), available only from the machine running the server.\n\n\n\n\n\n","title":"Perth.kanban_key!"},{"category":"function","location":"kanban/#Perth.kanban_add_card!","page":"Kanban","text":"kanban_add_card!(column, text) -> String\n\nAdd a card to column (id or name, case-insensitive) and return its id. Connected browsers update live.\n\n\n\n\n\n","title":"Perth.kanban_add_card!"},{"category":"function","location":"kanban/#Perth.kanban_move_card!","page":"Kanban","text":"kanban_move_card!(card_id, column; index = nothing) -> Bool\n\nMove a card to column (id or name). index is 1-based within the target column; omit it to append at the end. Connected browsers animate the move.\n\n\n\n\n\n","title":"Perth.kanban_move_card!"},{"category":"function","location":"kanban/#Perth.kanban_remove_card!","page":"Kanban","text":"kanban_remove_card!(card_id) -> Bool\n\nRemove a card from the shared kanban board.\n\n\n\n\n\n","title":"Perth.kanban_remove_card!"},{"category":"function","location":"kanban/#Perth.kanban_cards","page":"Kanban","text":"kanban_cards() -> Vector{NamedTuple}\n\nAll cards on the shared kanban board as (column, id, text) rows — Tables.jl-compatible, so kanban_cards() |> DataFrame just works.\n\n\n\n\n\n","title":"Perth.kanban_cards"},{"category":"function","location":"kanban/#Perth.kanban_columns","page":"Kanban","text":"kanban_columns() -> Vector{NamedTuple}\n\nColumns of the shared kanban board as (id, name, cards) rows.\n\n\n\n\n\n","title":"Perth.kanban_columns"},{"category":"function","location":"kanban/#Perth.kanban_from_project!","page":"Kanban","text":"kanban_from_project!(project; replace = false) -> Int\n\nPopulate the active kanban board from a Gantt Project (or a project name/id), creating one card per leaf task in backlog, doing or done according to its progress (0 / 1–99 / 100). Cards are linked to their tasks: dragging a linked card into the done column (or ticking it done) sets the task's progress to 100 in the Gantt — live, on every connected browser — and dragging it out reopens it.\n\nTasks that already have a linked card on the board are skipped, so the function is idempotent; pass replace = true to remove previously linked cards first. Returns the number of cards created.\n\nThe link holds across boards: editing a task in the Gantt updates its card even when that card sits on a board nobody has open — the board file is rewritten in place. Switching boards is not a way to lose the mirror.\n\n\n\n\n\n","title":"Perth.kanban_from_project!"},{"category":"function","location":"kanban/#Perth.kanban_board!","page":"Kanban","text":"kanban_board!(name) -> String\n\nSwitch the active board — for every connected browser at once (the board is one shared space, like a TV channel). Creates the board on first use. Also available in the UI (Board → Boards…) to the host.\n\n\n\n\n\n","title":"Perth.kanban_board!"},{"category":"function","location":"kanban/#Perth.kanban_boards","page":"Kanban","text":"kanban_boards() -> Vector{String}\n\nNames of the boards found in the Perth data directory (plus the active one). Switch with kanban_board! or Perth.kanban(\"name\").\n\n\n\n\n\n","title":"Perth.kanban_boards"},{"category":"function","location":"kanban/#Perth.kanban_delete_board!","page":"Kanban","text":"kanban_delete_board!(name) -> Bool\n\nDelete a board from disk — its cards, its activity log and its chat. Returns false if no board by that name exists, and throws if name is the active board: switch away first with kanban_board!, so nobody is left looking at a board that no longer exists. There is no undo — the files are removed.\n\nImages pasted into the board's cards are not deleted here. They live in a store shared by every board and addressed by content, so the same picture may belong to a card elsewhere; the ones nothing cites any more are collected on the next server start (see _asset_gc!).\n\nAlso in the UI (Board → Boards…) on the host machine only.\n\n\n\n\n\n","title":"Perth.kanban_delete_board!"},{"category":"function","location":"kanban/#Perth.kanban_alias!","page":"Kanban","text":"kanban_alias!(ip, name) -> Bool\n\nSet the display name for a machine, keyed by its IP address — the host-side rename (\"192.168.0.23\" → \"Paulo\"). The mapping applies everywhere the machine appears: cursors, presence chips and the card creator stamp. Pass an empty name to remove the alias. Also available in the UI, but only to the browser running on the server machine.\n\n\n\n\n\n","title":"Perth.kanban_alias!"},{"category":"function","location":"kanban/#Perth.kanban_log","page":"Kanban","text":"kanban_log(; limit = 50) -> Vector{NamedTuple}\n\nLatest activity on the shared kanban board as (at, by, text) rows, newest first — every add, move, edit, completion, archive and rename, with the actor's IP (or \"repl\"). Tables.jl-compatible.\n\n\n\n\n\n","title":"Perth.kanban_log"},{"category":"function","location":"kanban/#Perth.kanban_reset!","page":"Kanban","text":"kanban_reset!() -> Bool\n\nWipe the shared kanban board back to the default empty columns (backlog / doing / done), clearing cards and the archive. Host aliases survive — they describe machines, not cards. The reset is broadcast live and recorded in the activity log. Also available in the UI (Board → Reset board…) on the host machine only.\n\n\n\n\n\n","title":"Perth.kanban_reset!"}]
}
